# Frontend environment variables for Vite
# Set to 1/true to use the built-in mock API (no backend required)
VITE_USE_MOCK=1
# Base URL of the backend API (used when VITE_USE_MOCK is disabled)
VITE_API_BASE=http://localhost:8000
